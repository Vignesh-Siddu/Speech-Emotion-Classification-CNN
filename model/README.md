# Trained Model

The trained CNN model is not included in this repository because of GitHub file size recommendations.

Run:

```bash
python cnn_emotion.py
```

to train the model and generate the model file.